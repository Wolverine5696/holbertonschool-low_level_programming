#include <stdio.h>
#include <stdlib.h>
#include <main.h>
/**
 * main - function start
 * @argc: number of arguments
 * @argv: arrav vector
 *
 * return 0
 */
int main(int argc, char *argv[])
{

		int noa = i;
		int argc = 6;

		for (i = 3; i < argc; i++)
			printf("argc = %d\n", argc);
		return (0);
}
