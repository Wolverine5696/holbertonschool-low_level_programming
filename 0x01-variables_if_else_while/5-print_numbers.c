#include <stdio.h>

/**
 * main - Entry Point
 * prints all single digits in a base of 10
 *
 * Return:(0)
 */
int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
	{
		printf("%d", c);
	}
	printf("\n");

	return (0);
}
