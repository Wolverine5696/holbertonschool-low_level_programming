#include <stdio.h>
/**
 *main - extra code for alphabet in lower case
 *main - code for alphabet in lower case
 *
 *Return: 0
 *
 */
int main(void)
{
	int m;

	for (int m = 0; m <=26; m++)
		{
		putchar(97 + m);
		}
		putchar('\n');
		return (0);
}

