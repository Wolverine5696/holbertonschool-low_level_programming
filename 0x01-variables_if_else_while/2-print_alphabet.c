#include <stdio.h>
/**
 *main - etra code for alphabet in lower case
 *main - code for alphabet in lower case
 *
 *Return: 0
 *
 */
int main(void)
{
	int m;

	for (m = 0; m < 26; m++)
		{
		putchar(97 + m);
		}
		putchar('\n');
		return (0);
}

